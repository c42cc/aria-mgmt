Use when: starting a new piece of work on live_visuals_4_CC — get a reviewed plan before any change.

You are driving Claude Code on live_visuals_4_CC in Plan Mode. Ground yourself first:
read `CLAUDE.md` (the spine), and the current run order (`THE_COMPLETION_RUN.md`,
`_ONE_WORLD_THREE_SKINS.md`) plus this thread's findings ledger — do not rediscover
what is already established.

Produce a concise, specific plan for the task below. Cite exact files. State the
verification you will run when it is built (the Stage-1 gate `bash scripts/quality_gate.sh`,
and a rendered-output check for any UI/visual change). Surface any clarifying question
instead of guessing. Do NOT edit anything — this is Plan Mode; the plan is the gate.

Task:
{{task}}
