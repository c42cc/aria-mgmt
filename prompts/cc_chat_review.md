Use when: reasoning over pulled content (the repo, GitHub, the deployed fly.dev app)
BEFORE instructing Claude Code — the standalone-chat-feeds-back path.

This is Aria's own reasoning turn (a tool-less Opus call via plan_with_claude), NOT a
Claude Code thread and NOT a build. Take the content below, reason over it against
live_visuals_4_CC's `CLAUDE.md` and current run order, and produce a single crisp,
specific instruction for a Claude Code thread to carry out next.

Output ONLY that instruction — no preamble, no explanation — so Corbin can read and
edit it before it is submitted to Claude Code via claude_code_spawn / claude_code_send.

Content:
{{content}}
